{
  "name": "pwa",
  "version": "0.0.1",
  "scripts": {
    "prelint": "npx prettier --write .",
    "lint": "npx eslint **/*.js --fix || false",
    "predev": "npx yarn lint",
    "dev": "npx live-server --cors --port=5500",
    "deploy:netlify-live": "npx netlify deploy -p --dir=."
  },
  "pre-commit": "lint",
  "devDependencies": {
    "eslint": "^7.30.0",
    "eslint-config-google": "^0.14.0",
    "pre-commit": "^1.2.2"
  }
}
