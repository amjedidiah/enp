const newToast = (e, o = !0, s = selElem(e)) =>
  new bootstrap.Toast(s, { autohide: o });
const showToast = (e, o, s = selElem('.toast-body', e._element)) => {
  o && (s.innerHTML = o), e.show();
};
const hideToast = (e) => {
  e.hide();
};
